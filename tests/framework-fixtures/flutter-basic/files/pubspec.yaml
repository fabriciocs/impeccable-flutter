name: impeccable_flutter_fixture
description: Minimal Flutter fixture for Impeccable framework detection.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
